import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {artworks} from "@/data/artworks";export function generateStaticParams(){return artworks.map(a=>({id:a.id}))}export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const a=artworks.find(x=>x.id===id);if(!a)notFound();return <section className="section detail"><Link href="/shop">← Back to collection</Link><div className="detailGrid"><div className="detailImage"><Image src={a.image} alt={a.title} fill sizes="60vw"/></div><div><p className="eyebrow">{a.category}</p><h1>{a.title}</h1><p>By {a.artist}</p><h3>₹{a.price.toLocaleString("en-IN")}</h3><p className="muted">{a.description}</p><div className="specs"><span>Medium: {a.medium}</span><span>Size: {a.size}</span><span>Edition: {a.edition}</span></div><button className="button">Add to bag</button></div></div></section>}